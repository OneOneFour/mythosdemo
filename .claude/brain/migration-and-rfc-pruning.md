# Why docs/rfc/ holds two of six proposals

Salvaged from `docs/MIGRATION.md` (deleted — the flat-to-layered migration it
planned is long complete, and the file had drifted to actively contradict
binding decisions: it described the staged lift as reachable, the old
`rules/boons.js` as the grant layer, and the 9-beat tutorial as deferred, all
superseded by CLAUDE.md D1/D5/D10 and by the tutorial actually shipping).

The one passage worth keeping is the prescription that produced today's
curated `docs/rfc/` — the only surviving record of why four of the original
ten RFCs are absent:

> **Archive the RFCs.** `docs/rfc/` is 36,500 words across 10 files. Keep
> `00-REVIEW.md`, the final review, `BRIEF.md` and `REVIEW-CRITERIA.md` as the
> decision record; delete RFCs 01, 03, 05, 06 (rejected paradigms, superseded)
> and keep 02 and 04 as the sources the synthesis draws from.

`docs/rfc/SIDE-BY-SIDE.md` is the only surviving text of the four deleted
RFCs (01, 03, 05, 06) in excerpt form.
