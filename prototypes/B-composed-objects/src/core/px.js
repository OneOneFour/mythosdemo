/* Integer-pixel primitives. STUB: the leaf draw calls are no-ops here, because
   the brief puts art and rendering internals out of scope. The signatures are
   real so that render/treatments.js and render/looks.js are honest code. */
export function R(_g, _x, _y, _w, _h, _col) { /* stub: fillRect, integer args */ }
export function drawText(_g, _s, _x, _y, _col) { /* stub: 5x7 bitmap font */ }
export const mix = (a, _b, _k) => a;   // stub: channel lerp, returns a colour
